# vcharacter-sales Environment Configuration
#
# Copy this file to .env and fill in the values

# Network Configuration
# Set to 'testnet' or 'mainnet'
PUBLIC_VERUS_NETWORK=testnet

# URL of the other network's deployment (for the switch link in the UI)
# Leave empty if you only deploy one network
PUBLIC_SWITCH_NETWORK_URL=

# Service Identity for signing commitment challenges
# This is the private key (WIF format) for the service identity
#
# For testnet, use testidx@ identity:
#   1. Get the primary address: ./verus -chain=vrsctest getidentity testidx@
#   2. Look for "primaryaddresses" in the output
#   3. Export the private key: ./verus -chain=vrsctest dumpprivkey <primary_address>
#
# IMPORTANT: Keep this secret! Never commit to version control.
SERVICE_IDENTITY_WIF=YOUR_WIF_HERE

# Upstash Redis (required for cross-device wallet flow)
#
# Create a free database at https://console.upstash.com
# Then copy the REST API credentials from the database dashboard
#
# Without these, the app will only work on same-device flows
# (scanning QR on the same device where the browser is running)
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
